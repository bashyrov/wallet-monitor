[pytest]
testpaths = tests
addopts = -v --tb=short --timeout=30
# Kill any test running > 30s. Covers bcrypt-heavy auth tests while
# catching real hangs — e.g. a module import that opens a WebSocket.
timeout_method = thread
