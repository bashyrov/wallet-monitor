# Web framework
fastapi==0.135.1
uvicorn==0.41.0
starlette==0.52.1
python-multipart>=0.0.9

# Database
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg2-binary>=2.9.9      # PostgreSQL driver (production)
# aiosqlite>=0.19.0          # uncomment if switching to async SQLite locally

# Auth & crypto
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0,<5.0.0
cryptography>=42.0.0
eth-account>=0.11.0
python-binance
pyotp>=2.9.0

# Pydantic / settings
pydantic==2.12.5
pydantic-settings==2.13.1
pydantic_core==2.41.5
pydantic[email]>=2.0.0
python-dotenv==1.2.2

# HTTP clients (used by exchange providers)
httpx>=0.27.0
websockets>=13.0

# Fast JSON — C-backed, 5-10× the stdlib for encode/decode. Used on the
# hot path where books.json (4-5 MB) gets rewritten twice a second under
# heavy CPU pressure. Non-critical for everything else — codebase stays
# on stdlib `json` unless a call site is measurably CPU-bound on it.
orjson>=3.10.0

# Event loop acceleration — uvloop in place of asyncio selector loop.
# 2-4x throughput; critical on the fetcher container where 11 WS + orderbook
# pollers + arb compute share one loop. Optional: code falls back to stock
# asyncio if the wheel doesn't install (macOS arm64 can be finicky).
uvloop>=0.19.0; platform_system != "Windows"

# Redis — auth session cache (optional; code falls back to DB if unreachable)
redis>=5.0.0

# Test runner (only needed for CI / local dev, but harmless in prod image)
pytest>=8.0.0
pytest-asyncio>=0.23.0
# Hard-kills any test that exceeds the configured timeout (pytest.ini) —
# without this, a single test that tries to open a real WS from an import
# side-effect hangs the whole CI job for 12 minutes.
pytest-timeout>=2.3.0

# Misc
annotated-types==0.7.0
anyio==4.12.1
click==8.3.1
h11==0.16.0
idna==3.11
typing_extensions==4.15.0
ethereal-sdk>=0.1.4
